import { library } from "@fortawesome/fontawesome-svg-core";
import {
  faCircleExclamation,
  faPlay,
  faPause,
  faRotateRight,
  faSearch,
  faSpinner,
  faCheck,
  faXmark,
} from "@fortawesome/free-solid-svg-icons";
import { faSpotify } from "@fortawesome/free-brands-svg-icons";

// Add icons to the library so we can use them throughout the app
library.add(
  // Solid icons
  faCircleExclamation,
  faPlay,
  faPause,
  faRotateRight,
  faSearch,
  faSpinner,
  faCheck,
  faXmark,
  // Brand icons
  faSpotify
);
