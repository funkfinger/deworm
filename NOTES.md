# AI Instructions over time

1. ok, now we're going to restart from a 100% fresh Next.js application. We will keep only the PROJECT-CREATION-SOURCE folder which we will use later in the project creation. The step now is to return the project to a fresh Next.js application.
1. ok, now we can move the .nvm, .cursorrules, .env.local and README.md files into the root of the project
1. let's remove the folder PROJECT-CREATION-SOURCE
1. yes, lets commit and push
1. Let's create a docs folder and add a checklist of actions that need to happen to it. Refer to README.md and .cursorrules to understand what the project is and what technologies and design methods you need to use. Thanks
1. great, there are some lingering Next.js boilerplate files that should probably get removed or edited. Can you do that?
1. let's do the next item in the checklist
1. Ok, let's have Typescript target ES6
1. before we move forward, let's update implementation-checklist.md - also, let's rename that checklist.md for easier access.
1. Ok, let's fix the Husky pre-commit hook.
1. let's clean up what we did. we can look into the pre-commit hook later - let's either add it or not check it off of the checklist
1. I don't think that daisyUI is getting loaded - the site doesn't seem styled right. can you check that?
